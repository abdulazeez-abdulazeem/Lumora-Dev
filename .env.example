# OpenRouter configuration
PROVIDER=openrouter
OPENROUTER_API_KEY=your_openrouter_api_key_here
MODEL=cohere/north-mini-code:free

# Network bind (default localhost). Use 0.0.0.0 only on trusted networks.
LUMORA_BIND=127.0.0.1
