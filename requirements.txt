langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
python-dotenv>=1.0.0
rich>=13.0.0
langgraph-checkpoint
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.28.0
pytest>=8.0.0
cryptography>=42.0.0
Pillow>=10.0.0
# Browser automation (optional — install via requirements-browser.txt on local/dev
# or Debian/Ubuntu Docker images only). Not installed by default on cloud builders
# that lack manylinux wheels (Alpine/musl, restricted mirrors, etc.).
# pytesseract>=0.3.10  # optional for real OCR

# ---------------------------------------------------------------------------
# VERCEL TEST ONLY notes (nothing removed):
# - Core deps above are used on Vercel serverless Python.
# - Playwright remains in requirements-browser.txt for container/local use.
# ---------------------------------------------------------------------------
